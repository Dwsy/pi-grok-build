---
id: "2026-07-21-修复运行中展开Edit卡顿"
title: "修复 live turn 中展开 Edit/Write 导致整页卡顿"
status: "completed"
created: "2026-07-21"
updated: "2026-07-21"
category: "pager"
tags: ["performance", "edit", "scrollback", "live-turn"]
---

# Issue: 修复 live turn 中展开 Edit/Write 导致整页卡顿

## Goal

运行中展开（或自动展开）Edit/Write 可折叠区域时 TUI 不再持续卡顿；resume/idle 路径行为不变。

## 已确认根因

| 场景 | 行为 |
|---|---|
| Live turn + 展开 Edit 在视口 | 卡（折叠后立刻流畅） |
| Resume 同会话 + 展开/折叠 | 不卡 |
| `collapsed_edit_blocks` | ON（默认折叠；卡顿来自手动/untrusted 展开） |

Live 路径在 `TurnRunning` 时：

1. `tick_demand` 因 `!is_idle()` 保持 `TickDemand::Fast`（~30fps）
2. `tick()` 因 status spinner / running wave 强制整屏 `needs_redraw`
3. 展开 Edit 每帧重绘大块 styled diff → 输入与滚动被饿死

Resume 为 Idle，无上述热循环，故同样内容不卡。

## 修复

- `ScrollbackState::has_expanded_edit_in_viewport()`：检测视口内带 hunk/error 的 Expanded Edit
- `AppView::tick`：有重型展开 Edit 时，不因 scrollback 动画 / turn spinner 请求重绘
- `AppView::tick_demand`：同上，不单为 turn status / wave 维持 30fps
- ACP 流式：视口有展开 Edit 时 `min_draw_interval` 下限 120ms

内容更新、用户输入、交互路径仍正常请求绘制。

## 验收

- [x] 窄测 `has_expanded_edit_in_viewport` 通过
- [x] `cargo check -p xai-grok-pager-bin --bin grok-pi` 通过
- [ ] Live turn 手测：展开 Edit/Write 后滚动/输入仍流畅
- [ ] Resume 展开/折叠不回归

## Status

- **[2026-07-21]**: 根因确认；落地 redraw 抑制 + ACP 节流；单测 + binary check 通过。
- **[2026-07-21] 更新（compact A/B）**: 用户确认 **compact ON 不卡 / OFF 卡**。非 compact 路径启用 sticky headers，live turn 每帧 sticky 重绘是主成本。另：此前在展开 Edit 时压掉 spinner 重绘会冻住 `⠇`；spinner 帧率 divisor 4≈133ms 慢于 Pi Loader 80ms。
  - live turn 强制 `suppress_sticky_headers`（与 compact 同效）
  - 恢复 spinner 每帧推进；`SPINNER_DIVISOR=2` 对齐 Pi ≈80ms
  - 保留展开 Edit 时 ACP 120ms 下限与 wave 抑制
