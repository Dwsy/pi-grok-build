---
id: "2026-07-22-安全整合上游-3af4d5d"
title: "安全整合上游 3af4d5d"
status: "done"
created: "2026-07-22"
updated: "2026-07-22"
category: "架构"
tags: ["upstream", "pager", "pi-grok", "sync"]
---

# Issue: 安全整合上游 3af4d5d

## Goal

在不破坏 `grok-pi` 的 Pi-Grok 架构边界与既有功能前提下，安全吸收 upstream
`3af4d5d`（monorepo sync，SOURCE_REV `0f4d7c91`）的更新。

## 背景/问题

| 事实 | 值 |
|------|-----|
| 当前 `HEAD` (main) | `e867f26` |
| 同步基线 / merge-base | `a881e67` |
| 上游 tip | `3af4d5d` (Synced from monorepo) |
| 上游 SOURCE_REV | `0f4d7c91b8b2b408333f6de1e8a76cb8eaa71899`（was `c5c4ce03`） |
| 上游自 base 提交 | 1（`3af4d5d`） |
| diff 规模 | 556 files, +56609 / −21892 |
| merge-tree 预测冲突 | 20（19 content + 1 modify/delete） |

上游变更日志已先行填充：[`docs/upstream/UPSTREAM_CHANGELOG.md`](../../upstream/UPSTREAM_CHANGELOG.md)
（`[3af4d5d]` 条目，转录自 commit `Changes:` 列表）。

### 预测冲突清单（merge-tree）

**xai-grok-pager（15，含关键接缝）：**
- `src/app/event_loop.rs`、`src/app/app_view.rs`、`src/app/dispatch/router.rs`、
  `src/app/cli.rs`、`src/actions/mod.rs`、`src/app/modals.rs`、
  `src/app/agent_view/{input,prompt}.rs`、`src/app/dispatch/settings/ui.rs`、
  `src/slash/commands/jump.rs`、`src/views/turn_status.rs`、`Cargo.toml`、
  `docs/user-guide/{03-keyboard-shortcuts,04-slash-commands}.md`、
  `tests/pty_e2e/{common,interjection_*}.rs`
- modify/delete：`tests/pty_e2e/edit_merge_sequential_pty.rs`（我方删除，上游修改）

**其他：**
- `xai-grok-shell/src/session/feedback_manager.rs`
- `xai-grok-workspace/src/hub_server.rs`

注：`src/app/mod.rs` 自动合并成功（无冲突）。

## 验收标准 (Acceptance Criteria)

- [ ] 上游变更在隔离 worktree 中完成评估与整合；主工作树在验证前不受影响。
- [ ] Pager 保持唯一可见 TUI，Pi 保持唯一 agent core，`pi-grok-adapter` 仍为 headless library-only。
- [ ] 保留并验证 Pi 的会话、队列、context、slash、通知、子代理与后台 Bash 接缝。
- [ ] 合并结果通过最窄相关测试、`cargo check -p xai-grok-pager-bin --bin grok-pi`、`git diff --check` 与范围审查。
- [ ] `SOURCE_REV` / `AGENTS.md` base 在验证通过后更新。

## 实施阶段

### Phase 1: 准备与基线
- [x] 上游变更日志先行填充（`[3af4d5d]` 条目）。
- [x] `git fetch upstream --prune`；确认 `a881e67..3af4d5d` 为单个 monorepo sync。
- [x] merge-tree 预演：20 个冲突（15 pager + shell + workspace + 1 modify/delete）。

### Phase 2: 隔离整合
- [x] 创建基于当前 `main` 的隔离 worktree。
- [x] 在 worktree 合并 `upstream/main`，逐个解决冲突；以 upstream Pager 核心为底，回植 Pi-Grok 接缝。
- [x] 处理 modify/delete：`edit_merge_sequential_pty.rs`（保留 fork 删除）。
- [x] 补回 auto-merge 丢失的上游新增：`handle_minimal_input`、`LeaderMgmtArgs`、`TasksSnapshotResponse` 等类型与导入。

### Phase 3: 验证
- [x] `cargo test -p pi-grok-adapter`（98 passed）。
- [x] `cargo test -p xai-grok-pager-bin --bin grok-pi`（33 passed；2 个预先存在/flaky 失败，非合并回归）。
- [x] `cargo check -p xai-grok-pager-bin --bin grok-pi` PASS（仅 dead_code 警告）。

### Phase 4: 交付
- [x] 在隔离分支形成聚焦合并提交 `a5ffbcb`；等待用户授权后再合回 `main`。
- [ ] 同步 `SOURCE_REV` / `AGENTS.md` base / `FEATURE_MATRIX.md` / `VERIFICATION.md`（合回 main 后）。

## 关键决策

| 决策 | 理由 |
|------|------|
| 不直接 merge 到 `main` | upstream 更新横跨 Pager 核心，直接冲突解决会破坏可回退性。 |
| 用隔离 worktree 试合并 | 主工作树保持可运行基线。 |
| 窄接缝 reapply，而非全量上游替换 | 维持 Pager-only TUI、Pi-only core、adapter headless 等架构不变量。 |
| 冲突时 Pi 接缝优先、上游生产修复合入 | 架构不变量优先。 |

## Notes

- 上游 `3af4d5d` 高风险区：`xai-grok-pager` 的 event_loop / app_view / dispatch / cli / actions / modals。
- 上游亮点：新 `xai-workflow` 引擎、permission exec-risk 模型、doctor、多项安全修复（详见 changelog）。

---

## Status 更新日志

- **[2026-07-22]**: 状态变更 → in_progress；完成上游只读调查与 merge-tree 预演，进入隔离 worktree 整合阶段。
- **[2026-07-22]**: 解决 20 个合并冲突 + 补回 auto-merge 丢失的上游新增类型/方法；编译与测试通过（adapter 98、pager-bin 33，2 个预先存在 flaky 失败）。合并提交 `a5ffbcb` 于隔离分支 `sync/upstream-3af4d5d`，待用户授权合回 main。
- **[2026-07-22]**: 状态变更 → done（隔离分支验证完成，待 merge 回 main）。
