---
id: "2026-07-17-同步上游 Grok Build 至 98c3b24"
title: "同步上游 Grok Build 至 98c3b24"
status: "done"
created: "2026-07-17"
updated: "2026-07-17"
category: "架构"
tags: ["workhub", "upstream", "grok-build", "seams"]
---

# Issue: 同步上游 Grok Build 至 98c3b24

## Goal

将只读上游 `xai-org/grok-build` 从共同祖先 `c68e39f` 推进到 `upstream/main` (`98c3b24`)，在**不破坏 `grok-pi` 窄接缝**的前提下吸收 monorepo sync 变更，并通过 adapter / composition binary 验证。

## 背景/问题

| 事实 | 值 |
|------|-----|
| 当前 `HEAD` | `d003c94` (v0.0.4) |
| 共同祖先 / 声明 base | `c68e39f` |
| 上游 tip | `98c3b24` (Synced from monorepo) |
| 中间上游 commit | `8adf901` |
| 我方自 base 提交 | 22 |
| 上游自 base 提交 | 2 |
| 双方都改过的文件 | 65（`merge-tree` 中 62 个 `changed in both`） |
| 真实内容冲突 | **6 个文件**（其余可自动合并） |

上游两次 `Synced from monorepo` 覆盖 Pager event loop、settings modal 拆分、clipboard/trust、scrollback、MCP config、permission manager 等；与 Pi-Grok 在 `app/mod.rs`、`event_loop.rs`、`app_view.rs`、session/load、dashboard、queue 等接缝重叠。

**禁止**直接在 `main` 上 merge 上游后“碰运气”。策略：隔离 worktree + 保留 Pi 接缝 + 吸收上游生产修复。

### 预估真实冲突（merge-tree 有 `<<<<<<<`）

1. `README.md` — 文档叙述冲突（保留 Pi-Grok README，吸收 `SOURCE_REV` 说明）
2. `crates/codegen/xai-grok-pager/src/app/dispatch/router.rs`
3. `crates/codegen/xai-grok-pager/src/app/mod.rs` — **关键接缝**
4. `crates/codegen/xai-grok-pager/tests/pty_e2e/common.rs`
5. `crates/codegen/xai-grok-voice/src/config.rs`
6. `crates/codegen/xai-grok-workspace/src/permission/manager.rs`

### 可自动合并但仍需审查的关键路径

- `app/event_loop.rs`, `app/app_view.rs`, `app/actions.rs`, `app/effects/*`
- `app/dispatch/{dashboard,queue,session/load,session/lifecycle,task_result}.rs`
- `views/welcome/mod.rs`, `pager-bin/Cargo.toml`, `pager-bin/src/main.rs`
- `Cargo.lock`（merge 后应 `cargo generate-lockfile` 或由 check 刷新）

`pi-grok-adapter/` 与 `bin/grok-pi.rs` **不在上游树**，merge 本身不会删除它们；风险在 Pager 生命周期/API 漂移导致编译失败。

## 验收标准 (Acceptance Criteria)

- [x] WHEN merge 完成，系统 SHALL 包含 `upstream/main` (`98c3b24`) 的上游变更，且 `git merge-base HEAD upstream/main == upstream/main`。
- [x] WHERE Pi profile 启动，系统 SHALL 仍通过 `run_external` + headless `pi-grok-adapter`，不引入第二套 TUI / adapter UI 依赖。
- [x] IF 接缝与上游冲突，THEN 仅在声明的 external-profile / ACP / composition 接缝解决，不弱化 source-identity verifier。
- [x] `cargo test -p pi-grok-adapter` PASS。
- [x] `cargo check -p xai-grok-pager-bin --bin grok-pi` PASS。
- [x] `SOURCE_REV` / `AGENTS.md` base 与上游 tip 对齐；Issue / VERIFICATION 回写。

## 实施阶段

### Phase 1: 规划和准备
- [x] 分析共同祖先与双方 diff 范围
- [x] `merge-tree` 预演：6 文件真实冲突
- [x] 确定隔离 worktree 策略（不碰 `main` 工作区）
- [x] 创建本 Issue

### Phase 2: 执行
- [x] 创建 branch `sync/upstream-98c3b24` + 隔离 worktree
- [x] `git merge upstream/main`（允许冲突）
- [x] 解决 6 个冲突：优先保留 Pi-Grok 接缝语义，合入上游生产修复
- [x] 审查 auto-merge 的关键 Pager 文件（event_loop / app_view / session / dashboard / queue）
- [x] 修复因上游 API 漂移导致的编译错误
- [x] 刷新 lockfile / SOURCE_REV / baseline 元数据（如需要）

### Phase 3: 验证
- [x] `cargo test -p pi-grok-adapter`
- [x] `cargo check -p xai-grok-pager-bin --bin grok-pi`
- [x] 可选：`cargo test -p xai-grok-pager-bin --bin grok-pi`（4 pass / 1 pre-existing fail on both main and sync）
- [x] Diff 审查：adapter 仍 headless；无第二 TUI

### Phase 4: 交付
- [x] 更新本 Issue 状态与 Notes
- [x] 同步 `AGENTS.md` base；VERIFICATION 追加上游同步记录
- [x] 关闭旧 Issue `20260717-整合指定上游提交` 或标记 superseded
- [ ] 用户确认后再 merge 回 `main`（本 Issue 不自动 push）

## 关键决策

| 决策 | 理由 |
|------|------|
| 用 Git merge（有共同祖先）而非整树 patch 重放 | base=`c68e39f` 已在 fork 历史中；merge 保留双方提交语义 |
| 隔离 worktree | 避免污染当前 `main` 与未提交状态 |
| 冲突时 Pi 接缝优先、上游生产修复合入 | 架构不变量：Pager 唯一 TUI + Pi 唯一 core + adapter headless |
| 不直接 merge 到 `main` 直到验证 PASS | 未验证不说完成 |
| 关闭/取代旧 8adf901 Issue | 目标已推进到 tip `98c3b24`，旧独立 worktree 路径过期 |

## 遇到的错误

| 日期 | 错误 | 解决方案 |
|------|------|---------|
| | | |

## 相关资源

- [x] 架构: `NATIVE_GROK_TUI_ALIGNMENT.md`, `FEATURE_MATRIX.md`, `AGENTS.md`
- [x] 旧 Issue: `docs/issues/架构/20260717-整合指定上游提交.md`（superseded by this）
- [x] 上游: `https://github.com/xai-org/grok-build` @ `98c3b24`
- [x] 迁移说明: `README.md` § Migrating to a newer Grok baseline

## Notes

- `SOURCE_REV` 上游值为 `124d85bc5dc6e7805560215fcc6d5413944920e1`（monorepo SHA，不是 git commit）。
- 旧 patch 文件 `pi-grok-native-v4.0.0.patch` 针对更早 baseline；本次以 live merge 为准，patch 可在验证后再生。
- 已知 `./verify.sh` 基础设施 blocker 见 `VERIFICATION.md`，与本次同步结论分开陈述。

---



### Execution result (2026-07-17)

Branch: `sync/upstream-98c3b24` @ `5bd1312`
Worktree: `/Users/dengwenyu/Dev/AI/worktrees/pi-grok-sync-98c3b24`
`main` 未改动；待用户确认后 merge。

| Check | Result |
|---|---|
| `upstream/main` is ancestor of branch | PASS (`98c3b24`) |
| `SOURCE_REV` | `124d85bc5dc6e7805560215fcc6d5413944920e1` |
| `cargo test -p pi-grok-adapter` | PASS 46 |
| `cargo check -p xai-grok-pager-bin --bin grok-pi` | PASS (1 dead_code warning) |
| `cargo test -p xai-grok-pager-bin --bin grok-pi` | 4 pass / 1 fail (`--append-system-prompt` vs clap `--append-system-prompts`) — **pre-existing on main** |
| adapter headless | PASS (no ratatui/crossterm in adapter src) |

Conflict resolutions:
- README keep Pi-Grok
- router/mod/pty/permission take upstream (+ REQUESTER_GONE const)
- settings_modal monofile → upstream module split
- restore fork-deleted SetScreenMode/SetTimeline full path
- keep Pi theme `SettingValue::String` persistence in helpers/ui


## Status 更新日志

- **[2026-07-17]**: 状态变更 → `in_progress`，备注: 完成 diff 侦察与 merge-tree 预演；开始隔离 worktree 同步。

- **[2026-07-17]**: 状态变更 → `done`（分支验证完成，待 merge 回 main），备注: 隔离 worktree 同步+验证完成。
